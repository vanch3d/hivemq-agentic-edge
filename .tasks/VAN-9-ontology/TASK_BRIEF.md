# Task Brief: Domain Ontology & Spec Review

## Requirements

1. Perform a deep analysis of the HiveMQ Edge OpenAPI specification (`.docs/openapi-bundle.yaml`).
2. Produce a **spec review document** assessing completeness and correctness (building on prior findings about missing auth declarations, missing descriptions, etc.).
3. Produce an **agent-ready domain ontology document** that summarizes the domain model so that agents and humans can ask questions about it without re-reading the 8400-line YAML file.
